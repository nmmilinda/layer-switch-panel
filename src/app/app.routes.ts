import { Routes } from '@angular/router';
import { LayerSwitchPanelComponent } from './components/layer-switch-panel/layer-switch-panel.component';

export const routes: Routes = [
    {
        path: '',
        component: LayerSwitchPanelComponent
    }
];
